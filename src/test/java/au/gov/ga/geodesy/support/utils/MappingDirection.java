package au.gov.ga.geodesy.support.utils;

/**
 * Represents the direction of mapping, for example XML types to JPA entities
 * or vice versa.
 */
public enum MappingDirection {
    FROM_DTO_TO_ENTITY,
    FROM_ENTITY_TO_DTO
}